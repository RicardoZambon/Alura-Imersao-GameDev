let imageBackground;
let imageCharacter;
let imageEnemy;

let scenario;

let character;

function preload() {
    //imageBackground = loadImage('images/cenario/floresta.png');
    //imageCharacter = loadImage('images/characters/running.png');
    //imageEnemy = loadImage('images/enemies/little-drop.png');

    preLoad_Sounds();
}

function setup() {
    createCanvas(windowWidth, windowHeight);

    //scenario = new Scenario(imageBackground, 3);

    //character = new Character(imageCharacter, 60, 0.5, [3, 3], [220, 270], 3);
    //enemy = new Enemy(imageEnemy, width - 50, 0.5, [3, 6], [105, 100], 2, 6);

    //soundMusic.loop();
}

function keyPressed() {
    //if (key === 'ArrowUp') {
    //    character.jump();
    //}
}

function draw() {
    //scenario.show();

    //if (keyIsDown(RIGHT_ARROW)) {
    //    character.moveForward();
    //}

    //if (keyIsDown(LEFT_ARROW)) {
    //    character.moveBack();
    //}

    //character.show();
    //character.appliesGravity();
    //character.pullLeft();

    //enemy.show();

    //if (character.checkCollision(enemy)) {
    //    noLoop();
    //}


    //enemy.move();
}